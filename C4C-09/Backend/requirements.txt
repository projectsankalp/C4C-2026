# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Data validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0   # required by pydantic EmailStr (doctor login)

# --- MongoDB (async) ---
motor==3.6.0
pymongo==4.9.2          # provides bson; pulled in by motor but pinned explicitly

# --- SQLite (async, offline-first mirror) ---
SQLAlchemy==2.0.36
aiosqlite==0.20.0

# --- Auth / security ---
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
passlib==1.7.4

# --- HTTP client (SMS / FCM gateways) ---
httpx==0.28.1

# --- Scheduling (daily missed-followup cron) ---
APScheduler==3.11.0

# --- Tests ---
pytest==8.3.4
pytest-asyncio==0.25.0

# --- Voice Triage AI Pipeline ---
librosa==0.11.0
xgboost==3.2.0
scikit-learn==1.8.0
pyyaml==6.0.3
soundfile==0.13.1

