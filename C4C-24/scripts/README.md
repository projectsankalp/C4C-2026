Data preparation script

Usage:

Run the data preparation script to produce `master_dataset.csv` in the workspace root.

Requirements:
- Python 3.8+
- pandas, numpy

Command:

    python3 scripts/data_prep.py

Notes:
- The script is conservative and documents assumptions in comments.
