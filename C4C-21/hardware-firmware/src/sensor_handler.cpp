#include <Arduino.h>

namespace SensorHandler {
    void init() {
        // Init JSN-SR04T pins
    }

    float readLevel() {
        // Implement JSN-SR04T reading with noise filtering
        return 4.2; // Mock value
    }
}
